<% @farmers.each do |farmer| %> 
  <div class="square">
    <%= link_to image_tag(farmer.avatar.url(:medium).to_s, :class => "hover"), farmer_url(farmer.id), title:farmer.farmer_market_name %>
      </div>
     </div>  
     <%end%>
